/*import { TestBed, inject } from '@angular/core/testing';

import { ArchiveSettingsService } from './archive-settings.service';

describe('ArchiveSettingsService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ArchiveSettingsService]
    });
  });

  it('should be created', inject([ArchiveSettingsService], (service: ArchiveSettingsService) => {
    expect(service).toBeTruthy();
  }));
});
*/
